<div class="container-fluid last-background">
<div class="row card-deck">
    <div class="col-12 title"><h2 class="tltleDeck">Nos derniers produits</h2></div>
    {% for product in products %}
        <div class="col-xl-4 col-lg-6 col-md-6 col-sm-12 flex-wrap">
            <a href="/Product/describe/{{ product.id }}" class="cardLink">
                <div class="card">
                    <img src={{ '/assets/uploads/' ~ product.image }} class="card-img-top" alt="...">
                    <div class="card-body">
                        <h4 class="card-title-last">{{ product.name|capitalize }}</h4>
                        <p class="card-price">{{ product.price }} €</p>
                        <span class="badge-pill badge-primary">{{ product.category_name }}</span>
                        <span class="badge-pill badge-primary">{{ product.brand_name }}</span>
                    </div>
                </div>
            </a>
        </div>
    {% endfor %}
</div>
</div>
