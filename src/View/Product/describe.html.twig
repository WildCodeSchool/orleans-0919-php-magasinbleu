{% extends 'Home/index.html.twig' %}


{% block content %}

    <section class="describeProduct">
        <div class="container-fluid col-11">
            <div class="row justify-content-around">
                <div class="col-lg-4 col-xs-12 align-self-center text-center">
                    <img src="/assets/uploads/{{ product.image }}" alt="image qui représente le produit sélectionné"
                         class="img-thumbnail productImage">
                </div>
                <div class="col-lg-8 col-xs-12 p-2 align-self-center blocText">
                     <div class="p-0">
                        <div class="blocTop">
                            <div class="blocInfo col-8">
                                <div class="titleProduct">
                                    <h1 class="descriptionTitle">{{ product.name }}</h1>
                                </div>
                                <div class="sizeInfo">
                                    <p class="sizeDescribe badge badge-primary">{{ product.brand_name }}</p>
                                    <p class="sizeDescribe badge badge-primary">{{ product.category_name }}</p>
                                    <p class="sizeDescribe badge badge-primary">{{ product.reference }}</p>
                                </div>
                            </div>
                            <div class="blocPrice col-4">
                                <p class="productPrice text-left">{{ product.price }} €</p>
                            </div>
                        </div>
                        <p class="productDescription">{{ product.description }}</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
