{% extends 'layout.html.twig' %}

{% block title %}
    Au p'tit magasin bleu - Recherche produits
{%  endblock %}

{% block stylesheet %}
    <link rel="stylesheet" href="/assets/css/Product/index.css">
{% endblock %}

{% set urlStart = '/product/search/' %}

{% block navbar %}
    {% include 'Home/nav.html.twig' %}
{% endblock %}

{% block content %}
    <h1 class="titleProductUniverse">Résultat de la recherche : {{ searchTerm }}</h1>
    <p class="avail">
        {{ countProducts }} produits disponible
    </p>

    <div class="productDisplay">
        <div class="filterMenu bg-white">
            <a href="/product/indexUniverse/train">
                <h2 class="filterMenuTitle">Univers :</h2>
            </a>
            <ul>
                {% for universe in universes %}
                    <a href=
                       "{{- urlStart ~ '1/?universe=' ~ universe.name -}}
                        {%- if actualFilter.brand is not null -%}
                        {{- '&brand=' ~ actualFilter.brand -}}
                        {%- endif -%}
                        {%- if actualFilter.category is not null -%}
                        {{- '&category=' ~ actualFilter.category -}}
                        {%- endif -%}
                        {%- if actualFilter.available  is not null -%}
                        {{- '&available=' ~ actualFilter.available -}}
                        {%- endif -%}
                        {%- if searchTerm  is not null -%}
                        {{- '&search=' ~ searchTerm -}}
                        {%- endif -%}"
                       class="filterLinks">
                        <li>{{ universe.name }}</li>
                    </a>
                {% endfor %}
            </ul>

            <a href=
               "{{- urlStart ~ '1/?search=' ~ searchTerm -}}
                {%- if actualFilter.universe is not null -%}
                {{- '&universe=' ~ actualFilter.universe -}}
                {%- endif -%}
                {%- if actualFilter.category is not null -%}
                {{- '&category=' ~ actualFilter.category -}}
                {%- endif -%}">
                <h2 class="filterMenuTitle">Marques :</h2>
            </a>
            <ul>
                {% for brand in brands %}
                    <a href=
                       "{{- urlStart ~ '1/?search=' ~ searchTerm ~ '&brand=' ~ brand.name -}}
                        {%- if actualFilter.universe is not null -%}
                        {{- '&universe=' ~ actualFilter.universe -}}
                        {%- endif -%}
                        {%- if actualFilter.category is not null -%}
                        {{- '&category=' ~ actualFilter.category -}}
                        {%- endif -%}
                        {%- if actualFilter.available  is not null -%}
                        {{- '&available=' ~ actualFilter.available -}}
                        {%- endif -%}"
                       class="filterLinks">
                        <li>{{ brand.name }}</li>
                    </a>
                {% endfor %}
            </ul>
            <a href=
               "{{- urlStart ~ '1/?search=' ~ searchTerm -}}
                {%- if actualFilter.universe is not null -%}
                {{- '&universe=' ~ actualFilter.universe -}}
                {%- endif -%}
                {%- if actualFilter.category is not null -%}
                {{- '&brand=' ~ actualFilter.brand -}}
                {%- endif -%}">
                <h2 class="filterMenuTitle">Categories :</h2>
            </a>
            <ul>
                {% for category in categories %}
                    <a href=
                       "{{- urlStart ~ '1/?search=' ~ searchTerm ~ '&category=' ~ category.name -}}
                        {%- if actualFilter.universe is not null -%}
                        {{- '&universe=' ~ actualFilter.universe -}}
                        {%- endif -%}
                        {%- if actualFilter.brand is not null -%}
                        {{- '&brand=' ~ actualFilter.brand -}}
                        {%- endif -%}
                        {%- if actualFilter.available  is not null -%}
                        {{- '&available=' ~ actualFilter.available -}}
                        {%- endif -%}"
                       class="filterLinks">
                        <li>{{ category.name }}</li>
                    </a>
                {% endfor %}
            </ul>

            <a href=
               "{{- urlStart ~ '1/?search=' ~ searchTerm -}}
                {%- if actualFilter.universe is not null -%}
                {{- '&universe=' ~ actualFilter.universe -}}
                {%- endif -%}
                {%- if actualFilter.category is not null -%}
                {{- '&brand=' ~ actualFilter.brand -}}
                {%- endif -%}
                {%- if actualFilter.category is not null -%}
                {{- '&category=' ~ actualFilter.category -}}
                {%- endif -%}">
                <h2 class="filterMenuTitle">Disponibilité :</h2>
            </a>
            <ul>
                <a href=
                   "{{- urlStart ~ '1/?search=' ~ searchTerm ~ '&' -}}
                    {%- if actualFilter.available is null -%}
                    {{- 'available=' ~ 'true&' -}}
                    {%- endif -%}
                    {%- if actualFilter.brand is not null -%}
                        {{- 'brand=' ~ actualFilter.brand ~ '&' -}}
                    {%- endif -%}
                    {%- if actualFilter.category is not null -%}
                        {{- 'category=' ~ actualFilter.category ~ '&' -}}
                    {%- endif -%}
                    {%- if actualFilter.universe is not null -%}
                    {{- '&universe=' ~ actualFilter.universe -}}
                    {%- endif -%}" class="filterLinks">
                    <li>En stock</li>
                </a>
                <a href=
                   "{{- urlStart ~ '1/?search=' ~ searchTerm -}}
                    {%- if actualFilter.universe is not null -%}
                    {{- '&universe=' ~ actualFilter.universe -}}
                    {%- endif -%}
                    {%- if actualFilter.category is not null -%}
                    {{- '&brand=' ~ actualFilter.brand -}}
                    {%- endif -%}
                    {%- if actualFilter.category is not null -%}
                    {{- '&category=' ~ actualFilter.category -}}
                    {%- endif -%}" class="filterLinks">
                    <li>Tout</li>
                </a>
            </ul>
        </div>

        <div class="container-fluid">
            <div class="card-deck">
                {% for product in products %}
                    <div class="col-xl-4 col-lg-6 col-md-6 col-sm-12 flex-wrap">
                        <a class="cardLink" href="/Product/describe/{{ product.id }}">
                            <div class="card">
                                <img src="{{ '/assets/uploads/' ~ product.image }}" class="card-img-top" alt="...">
                                <div class="card-body">
                                    <h4 class="card-title">{{ product.name|capitalize}}</h4>
                                    <p class="card-text">{{ product.price }} €</p>
                                    <p class="card-text">Catégorie: {{ product.category_name }}</p>
                                    <p class="card-text">Marque: {{ product.brand_name }}</p>
                                    <p class="card-text inStock">
                                        {% if product.availability == 0 %}
                                            Rupture de stock
                                        {% else %}
                                            Disponible
                                        {% endif %}
                                    </p>
                                </div>
                            </div>
                        </a>
                    </div>
                {% endfor %}
            </div>
            <nav aria-label="Page navigation example" class="navPage">
                <ul class="pagination">
                    {%  for i in 1..countPages %}
                        <li class="page-item">
                            <a class="page-link" href=
                            "{{- urlStart ~ i ~ '/?search=' ~ searchTerm -}}
                            {%- if actualFilter.universe is not null -%}
                            {{- '&universe=' ~ actualFilter.universe -}}
                            {%- endif -%}
                            {%- if actualFilter.available is not null -%}
                            {{- '&available=' ~ 'true' -}}
                            {%- endif -%}
                            {%- if actualFilter.brand is not null -%}
                            {{- '&brand=' ~ actualFilter.brand -}}
                            {%- endif -%}
                            {%- if actualFilter.category is not null -%}
                            {{- '&category=' ~ actualFilter.category -}}
                            {%- endif -%}">{{ i }}
                            </a>
                        </li>
                    {% endfor %}
                </ul>
            </nav>
        </div>
    </div>

{% endblock %}

{% block footer %}
    {% include "/Home/footer.html.twig" %}
{% endblock %}
